version: "3.8"

services:
  site:
    build: .
    ports:
      - "8000:8000"
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: sh -c "npx decap-server & npm run develop -- -H 0.0.0.0"
